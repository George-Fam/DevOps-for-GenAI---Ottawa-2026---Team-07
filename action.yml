name: Yami
description: Governed infrastructure release agent

inputs:
  policy:
    required: true
  mode:
    required: true
    default: live

outputs:
  decision:
    description: SAFE_FIX, HUMAN_REVIEW or BLOCK
  risk-score:
    description: Calculated risk score

runs:
  using: docker
  image: Dockerfile
