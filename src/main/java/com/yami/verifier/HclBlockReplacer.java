package com.yami.verifier;

import java.nio.file.Path;

public class HclBlockReplacer {

    public void replace(Path terraformFile, String resourceAddress, String replacementBlock) {
        throw new UnsupportedOperationException(
            "locate the HCL block for resourceAddress (hcl4j) and replace it with replacementBlock. "
            + "this is the 20:00 spike - the one operation the whole plan pivots on");
    }
}
